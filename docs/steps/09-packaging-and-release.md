# Step 09 - Packaging and Release

## Goal
Prepare builds for distribution.

Desktop:

- use PyInstaller for executable
- bundle assets with executable

Mobile (future):

- evaluate migration path if needed (e.g., Godot)
- if staying Kivy, use Buildozer pipelines

Release checklist:

- version tag
- changelog update
- smoke test on target OS
